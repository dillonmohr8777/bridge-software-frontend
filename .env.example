# Phase 3 uses the in-memory adapter until Miraj publishes an inspectable staging origin.
# Set only a versioned API origin. Do not put tokens or service keys in this file.
NEXT_PUBLIC_BRIDGE_API_BASE=

# Add these only after Miraj approves the Supabase project and data/auth contracts.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Visual direction lock for staging preview sites only.
# Unified review (bridge-connected-signal.netlify.app) must stay on Modern Network.
# NEXT_PUBLIC_DIRECTION_LOCK=network
