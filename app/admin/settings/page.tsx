"use client";
import { useAuth } from "@/components/auth/AuthProvider";
export default function AdminSettingsPage() { const { user } = useAuth(); return <><div className="dashboard-heading"><div><p className="eyebrow">Account</p><h1>Settings</h1><p className="lede">Your administrative account information.</p></div></div><form className="content-card admin-form" onSubmit={(e) => e.preventDefault()}><label htmlFor="name">Display name</label><input id="name" defaultValue={user?.profile?.displayName ?? ""} /><label htmlFor="email">Email address</label><input id="email" value={user?.email ?? ""} readOnly /><label htmlFor="phone">Phone number</label><input id="phone" defaultValue={user?.profile?.phone ?? ""} /><p className="boundary-note">Profile updates remain read-only until the settings endpoint is available.</p><button className="button primary" disabled>Save changes</button></form></>; }
